<UserControl x:Class="LeitostrapV7.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Background="#000000">
    <UserControl.Resources>
        <SolidColorBrush x:Key="Bg" Color="#000000"/>
        <SolidColorBrush x:Key="Bg2" Color="#050505"/>
        <SolidColorBrush x:Key="CardBg" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="Bdr" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="T1" Color="White"/>
        <SolidColorBrush x:Key="T2" Color="#555555"/>
        <SolidColorBrush x:Key="T3" Color="#333333"/>


        <Style x:Key="CardBtn" TargetType="Button" BasedOn="{StaticResource ModernBtn}"/>
    </UserControl.Resources>


    <Grid Margin="20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>


            <TextBlock Grid.Column="0" Text="Console" FontSize="18" FontWeight="SemiBold" 
                       Foreground="White" VerticalAlignment="Center"/>


            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="CopyLogBtn" Style="{StaticResource CardBtn}" 
                        Click="CopyLogBtn_Click" Margin="0,0,6,0">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Document20" FontSize="11" Margin="0,0,5,0"/>
                        <TextBlock Text="Copy Log" FontSize="10"/>
                    </StackPanel>
                </Button>
                <Button x:Name="OpenLogBtn" Style="{StaticResource CardBtn}" 
                        Click="OpenLogBtn_Click" Margin="0,0,6,0">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="FolderOpen20" FontSize="11" Margin="0,0,5,0"/>
                        <TextBlock Text="Open Log" FontSize="10"/>
                    </StackPanel>
                </Button>
                <Button x:Name="ClearBtn" Style="{StaticResource CardBtn}" 
                        Click="ClearBtn_Click">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="Dismiss20" FontSize="11" Margin="0,0,5,0"/>
                        <TextBlock Text="Clear" FontSize="10"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>


        <Border Grid.Row="2" Background="#0A0A0A" BorderBrush="#1A1A1A" BorderThickness="1" 
                CornerRadius="10" Padding="0">
            <Grid>
                <ScrollViewer x:Name="ConsoleScroll" VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Auto">
                    <TextBox x:Name="ConsoleOutput" 
                             Background="Transparent" 
                             BorderThickness="0"
                             Foreground="#AAAAAA"
                             FontFamily="Cascadia Mono, Consolas, Courier New"
                             FontSize="12"
                             IsReadOnly="True"
                             TextWrapping="NoWrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled"
                             Padding="14,10"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
